#!/usr/bin/env bash
set -eu

# This script is the base for uninstalling a new profile.
# It is intended to be copied and modified for each new profile.
# When removing something, be cautious to only delete those that were created by the install script.
