#!/usr/bin/env sh
set -u

# This script is the base for installing a new profile.
# It is intended to be copied and modified for each new profile.
# It must be idempotent and handle re-execution gracefully.
